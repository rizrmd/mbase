{
  "name": "maldevta",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run scripts/dev.ts",
    "dev:backend": "cd backend && bun run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "prod": "bun run scripts/prod.ts",
    "prod:build": "cd frontend && npm run build",
    "prod:start": "cd backend && bun run start",
    "prod:preview": "cd frontend && npm run preview"
  },
  "devDependencies": {
    "@types/bun": "^1.3.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "module": "index.ts"
}
